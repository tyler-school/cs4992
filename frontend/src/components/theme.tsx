import { createTheme } from '@mui/material/styles';

  const theme = createTheme({
    palette: {
      primary: {
        main: '#ffffff', // white
      },
      secondary: {
        main: '#f15025', //orange
      },
      // Add more custom colors as needed
    },
    // Add other customizations to the theme if desired
  });
  
  export default theme;
